# Exclude uploads directory from Vercel builds
uploads
uploads/saves
uploads/saves/*.cok

# Exclude public uploads (images) from build
public/uploads
public/uploads/cities
public/uploads/cities/*

# Exclude development files
.env.local
.env.development.local
.env.test.local

# Exclude package-lock.json since we're using pnpm
package-lock.json
yarn.lock

# Exclude git directory
.git

# Exclude node_modules (should be installed by Vercel)
node_modules

# Exclude any nested directories that might cause recursion
**/nextjs-typescript-starter 